# FairLLM Sports Edge (Midpoint)

A minimal, runnable scaffold for your CS471 FairLLM agent.

## Quickstart
1. **Open in VS Code**
   - File → Open Folder… → `fairllm-sports-edge`

2. **Create a virtual environment**
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate  # Windows: .venv\Scripts\activate
   python -m pip install --upgrade pip
   pip install -r requirements.txt
